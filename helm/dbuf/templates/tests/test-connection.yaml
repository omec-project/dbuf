apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbuf.fullname" . }}-test-connection"
  labels:
    {{- include "dbuf.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbuf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
